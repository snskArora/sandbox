name: Dynamic Matrix Example

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Select the type of release bump'
        required: true
        type: 'choice'
        options:
          - patch
          - minor
          - major
          - rc
        default: rc
  pull_request:
    types: [closed]
    branches:
      - main
  push:

jobs:
  prepare-matrix:
    runs-on: ubuntu-latest
    steps:
    # - name: checkout
    #   uses: actions/checkout@v4

    - name: Generate token
      id: generate_token
      uses: tibdex/github-app-token@v1
      with:
        app_id: ${{ secrets.APP_ID }}
        private_key: ${{ secrets.APP_PRIVATE_KEY }}

    # - name: tag bump
    #   id: tag-bump
    #   shell: bash
    #   run: |
    #     # recent_tag=$(ls .git/refs/tags | sort -Vr | head -n1)
    #     recent_tag=$(curl -s "https://api.github.com/repos/${{ github.repository }}/tags" | grep '"name":' | sed -E 's/.*"name": "([^"]+)".*/\1/' | sort -Vr | head -n1)
    #     echo "--- $recent_tag"
    #     new_tag=$(bash .github/tag-bump.sh $recent_tag ${{ inputs.bump }})
    #     echo "new tag: $new_tag"
    #     echo "new_tag=$new_tag" >> $GITHUB_OUTPUT

    # - name: changelog-ci
    #   uses: saadmk11/changelog-ci@v1.1.2
    #   id: changelog-ci
    #   with:
    #     release_version: ${{ steps.tag-bump.outputs.new_tag }}

    # - name: Create Release
    #   id: create_release
    #   uses: actions/create-release@v1
    #   env:
    #     GITHUB_TOKEN: ${{ steps.generate_token.outputs.token }} # This token is provided by Actions, you do not need to create your own token
    #   with:
    #     tag_name: ${{ steps.tag-bump.outputs.new_tag }}
    #     release_name: Release ${{ steps.tag-bump.outputs.new_tag }}
    #     body: ${{ steps.changelog-ci.outputs.changelog }}

    - id: test
      run: |
        echo "test1=val1" >> $GITHUB_OUTPUT
        echo "test2=val2" >> $GITHUB_OUTPUT

    - id: issue
      uses: trstringer/manual-approval@v1
      with:
        secret: ${{ steps.generate_token.outputs.token }}
        approvers: snskArora
        minimum-approvals: 1
        issue-body: |
            
            In sagittis non tortor ut convallis. Nulla placerat ipsum a porta porttitor. In odio eros, dignissim sed maximus ac, congue et enim. Nunc elementum auctor purus. Etiam pretium, libero vel luctus maximus, nibh turpis malesuada urna, a volutpat nunc tortor eget felis. Vestibulum sed tortor velit. Duis vitae maximus dolor. In hac habitasse platea dictumst. Suspendisse vel mattis enim. Duis vitae leo mauris. Morbi non tortor metus. Curabitur cursus convallis diam cursus tristique. Vivamus malesuada est est, vitae porttitor libero tristique sit amet. Aliquam egestas elementum risus, ac vehicula lectus tempus hendrerit. Maecenas condimentum massa id nunc euismod, ac cursus ex tempor. Vestibulum interdum purus vel pretium gravida.

            Vivamus vestibulum placerat pellentesque. In quam ipsum, facilisis ac faucibus a, scelerisque vitae tortor. Praesent imperdiet ultrices erat ac consectetur. Donec sit amet lorem leo. Aliquam erat volutpat. Phasellus maximus nunc id maximus elementum. Nulla sagittis, enim sit amet consequat ultrices, nibh turpis consectetur dolor, ac gravida metus justo id neque. Donec eget tempus tellus, et vestibulum enim. Vestibulum ante ipsum primis in faucibus orci luctus et ultrices posuere cubilia curae; Suspendisse et ex e
        target-repository: first_go_project
        target-repository-owner: snskArora
        
    - name: Display all outputs as JSON
      run: |
        echo "${{ steps.issue.outputs.issue-number }}"
        echo "${{ steps.issue.outputs.issue-url }}"
        echo "${{ steps.issue.outputs.approval-status }}"
        echo "--------------"
        echo "${{ toJSON(steps.issue.outputs) }}"


